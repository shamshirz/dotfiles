Need to install elm-format manually for not
VS Code can take advantage of it

sudo npm install -f elm-format


Also need to get my packages from VS code
Also need to get the correct settings, for some reason the settings im copying with the update script aren't working

